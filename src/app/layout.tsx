import type { Metadata, Viewport } from "next";
import { Geist } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Cosmic Idle · Energy Empire",
  description:
    "An idle clicker game set in space. Click, build, and expand your energy empire across the cosmos. Prestige to grow even stronger.",
  keywords: ["idle game", "clicker", "space", "incremental", "energy"],
  icons: {
    icon: "/favicon.svg",
  },
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
  themeColor: "#020617",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="h-full">
      <body className={`${geistSans.variable} antialiased h-full`}>
        {children}
      </body>
    </html>
  );
}
